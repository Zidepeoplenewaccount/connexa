# Legacy Server Folder

This folder contains an old static Express server example.

Important:

- It is not the production backend API.
- It does not replace FastAPI services.
- It is not part of the active frontend runtime path.

For current development, use the root Vite app and backend service repositories.
