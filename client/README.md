# Legacy Client Folder

This folder is legacy scaffolding and is not the active frontend source of truth.

Use the root app instead:

- source: `../src`
- scripts: `../package.json`
- build tool: Vite at repository root

Do not add new features here unless the team explicitly decides to migrate architecture.
